import React, { memo } from 'react';
import { Card, Center, Heading, Spinner } from '@chakra-ui/react';

export const Register: React.FC = memo(() => {
  return (
    <>
      <Center mt="10">
        <Heading as="h1">名刺新規登録</Heading>
      </Center>
    </>
  );
});
